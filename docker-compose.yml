services:
  salon-daily:
    build: .
    container_name: salon-daily
    ports:
      - "4000:4000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - PORT=4000
